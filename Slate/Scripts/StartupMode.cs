namespace JCMG.Slate
{
	/// <summary>
	/// Indicates whether the UI should be shown or hidden on load and when.
	/// </summary>
	public enum StartupMode
	{
		HideOnAwake = 0,
		HideOnStart = 1,
		ShowOnAwake = 2
	}
}
